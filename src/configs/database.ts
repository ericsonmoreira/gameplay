const DATA_BASE_NAME = '@gameplay';

// @app:collection

const COLLECTION_USERS = `${DATA_BASE_NAME}:users`;
const COLLECTION_APPOINTMENTS = `${DATA_BASE_NAME}:appointments`;

export { COLLECTION_USERS, COLLECTION_APPOINTMENTS };
